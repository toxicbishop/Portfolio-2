# Example Environment Variables
# Copy this file to .env and fill in your actual keys

# Rate Limiting (Optional: Use Upstash Redis for distributed rate limiting)
UPSTASH_REDIS_REST_URL=your_upstash_redis_url
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token

# API Keys (Move any hard-coded keys here)
# Example: GITHUB_TOKEN=your_github_token_here
# Example: CONTACT_EMAIL_SERVICE_KEY=your_key_here

# Security Note: 
# 1. DO NOT prefix sensitive keys with NEXT_PUBLIC_ unless they must be accessible client-side.
# 2. Add .env to your .gitignore to prevent leaking secrets.
# 3. Rotate these keys periodically for better security.
